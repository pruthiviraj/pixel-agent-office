# LinkedIn launch — draft posts

Pick one, drop in a 10–20s screen recording of the office (`?demo=1` or a real sprint),
and link the repo. Short video > screenshot for reach.

---

## Option A — the hook (recommended)

> I gave a team of AI agents a pixel-art office to work in. 🏢

> You hand it a goal. A **PM agent** breaks it into tasks. **Developer agents** build them in parallel. A **QA agent** runs the tests and votes PASS/FAIL — and if it fails, it bounces back to the developer with the reason. You watch the whole thing happen in a retro office that looks like a 90s game.

> It even *learns*: every QA failure becomes a one-line lesson that's fed into the next sprint, so the team stops repeating mistakes.

> It's open source (MIT), pure Node, no build step, and works on any codebase via simple "profiles". Built on Claude Code.

> 👉 github.com/pruthiviraj/pixel-agent-office

> What would you point it at first?

> #AI #AgenticAI #ClaudeCode #DeveloperTools #OpenSource

---

## Option B — the builder's angle

> Most "AI agent" demos are a single chat box. I wanted to *see* a team work.

> So I built **Pixel Agent Office** — a real PM → dev → QA orchestration loop for Claude Code agents, rendered as a live pixel-art office:

> • PM decomposes your epic into a dependency-ordered task graph
> • Devs implement in parallel (with a concurrency cap)
> • QA verifies each piece and gates it — failures retry with feedback
> • The team accumulates "lessons learned" across sprints

> Point it at any repo with one command. Stack-agnostic via profiles (ships with a generic one + a Salesforce example). MIT licensed.

> Repo + 60-second demo 👇
> github.com/pruthiviraj/pixel-agent-office

> #AIagents #ClaudeCode #SoftwareEngineering #OpenSource #DevTools

---

## Option C — one-liner + video

> A PM, three developers, and a QA tester walk into a pixel office… and ship your backlog. 🧑‍💻🧪

> Open-source multi-agent orchestration for Claude Code, with a virtual office you can actually watch. MIT.

> github.com/pruthiviraj/pixel-agent-office

---

### Asset checklist before posting
- [ ] 10–20s screen recording of the office (run `node server.js`, open `?demo=1`, or record a real `--dry-run`).
- [ ] Add that GIF/MP4 to the repo README (`docs/office.png` / `.gif`) so the GitHub page sells itself too.
- [ ] Pin the repo on your GitHub profile.
- [ ] Reply to your own post with the "how it works" architecture diagram for the algorithm crowd.
